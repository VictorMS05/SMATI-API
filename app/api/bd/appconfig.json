{
    "conexionDB": {
        "host": "${DB_HOST}",
        "port": 3306,
        "user": "${DB_USER}",
        "password": "${DB_PASSWORD}",
        "database": "${DB_NAME}",
        "multipleStatements": true,
        "timezone": "utc"
    }
}
